# Backend origin (no trailing slash). Used for API calls and uploaded assets.
# Leave empty in production when the frontend is served from the same host as the API.
VITE_API_URL=http://localhost:3004
